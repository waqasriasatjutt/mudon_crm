<?xml version="1.0" encoding="UTF-8"?>
<odoo>
    <!--
        Inherits the standard Pipeline form and lays out all 7 stages
        worth of Mudon-specific fields across grouped sections. Per-
        pipeline visibility is driven by the computed
        `mudon_pipeline_kind` marker; the stage-currentness of each
        group is driven by lookups against the seeded stage xmlids
        via the lead's `stage_id` field name.
    -->
    <record id="mudon_crm_lead_view_form" model="ir.ui.view">
        <field name="name">crm.lead.form.mudon</field>
        <field name="model">crm.lead</field>
        <field name="inherit_id" ref="crm.crm_lead_view_form"/>
        <field name="arch" type="xml">
            <!--
                Inject ALL Mudon stage fields directly into the main
                opportunity body, right after the phone block (anchored
                at lost_reason_id which sits one line below phone in
                Odoo 19's stock form). Each stage's fields show on top
                when the lead is on that stage — there is no separate
                notebook tab (removed in v19.0.1.6.0 per client feedback).

                The 4 fields MService / MCity / MPriority / MBudget
                are flagged required="..." at the view layer (UX —
                user sees the red border immediately) AND validated
                in the backend write() override (safety — blocks
                stage drag-and-drop without these set).
            -->
            <xpath expr="//notebook" position="before">
                <!-- Invisible driver fields for the Mudon logic below. -->
                <field name="mudon_pipeline_kind" invisible="1"/>
                <field name="mudon_card_color_hint" invisible="1"/>
                <field name="mudon_kanban_priority_rank" invisible="1"/>
                <field name="mudon_stage_kind_current" invisible="1"/>
                <field name="mudon_is_funnel_stage" invisible="1"/>
                <field name="mudon_show_city_other" invisible="1"/>
                <field name="mudon_budget_currency_id" invisible="1"/>
                <field name="company_currency" invisible="1"/>

                <!--
                    STAGE FIELDS: current stage on TOP, completed stages
                    stacked BELOW (nothing vanishes). Blocks are laid out
                    in reverse funnel order and each shows for its stage
                    AND every later stage, so the current (highest) stage
                    always renders first and earlier ones follow beneath.
                -->

                <!-- Lost (current) -->
                <group string="Lost" class="mudon_sec mudon_sec_lost"
                       invisible="mudon_stage_kind_current != 'lost'">
                    <field name="mudon_lost_reason_id" string="Lost Reason"
                           options="{'no_create_edit': True}" readonly="1"/>
                </group>

                <!-- WON / SPA Signed -->
                <group invisible="mudon_stage_kind_current != 'won'">
                    <group string="WON — closing details" class="mudon_sec mudon_sec_won">
                        <field name="mudon_closing_amount" groups="mudon_crm.group_mudon_financial_data"/>
                        <field name="mudon_developer_id"
                               options="{'no_create_edit': True}"/>
                        <field name="mudon_project_id"
                               domain="[('pipeline_kind', 'in', [mudon_pipeline_kind, 'any'])]"
                               options="{'no_create_edit': True}"/>
                        <!--
                            Field change 10: the client enters a percentage of
                            the Closing Amount now. The money figure stays on
                            screen, read-only, because that is the number the
                            dashboards and the deal register report.
                        -->
                        <field name="mudon_commission_pct" groups="mudon_crm.group_mudon_financial_data"/>
                        <field name="mudon_commission" groups="mudon_crm.group_mudon_financial_data"/>
                        <field name="mudon_commission_type_id"
                               options="{'no_create_edit': True}"/>
                        <field name="mudon_handover_type"/>
                        <field name="mudon_reason_to_win"/>
                    </group>
                    <group string="Billing &amp; collection" class="mudon_sec mudon_sec_won">
                        <field name="mudon_invoiced_amount" groups="mudon_crm.group_mudon_financial_data"/>
                        <field name="mudon_invoiced_date" groups="mudon_crm.group_mudon_financial_data"/>
                        <field name="mudon_collected_amount" groups="mudon_crm.group_mudon_financial_data"/>
                        <field name="mudon_collected_date" groups="mudon_crm.group_mudon_financial_data"/>
                    </group>
                    <group string="Funnel triggers" class="mudon_sec mudon_sec_won">
                        <field name="mudon_need_invoice"/>
                        <field name="mudon_title_deed_required"/>
                        <field name="mudon_citizenship_required"
                               invisible="mudon_pipeline_kind != 'turkey'"/>
                        <field name="mudon_residence_required"
                               invisible="mudon_pipeline_kind != 'uae'"/>
                        <field name="mudon_furniture_required"/>
                    </group>
                    <group string="Admin Funnel — auto-created tasks" colspan="2">
                        <field name="mudon_admin_task_ids" nolabel="1" readonly="1">
                            <list>
                                <field name="name"/>
                                <field name="state"/>
                                <field name="assigned_user_id"/>
                            </list>
                        </field>
                    </group>
                    <group string="After-Sales Funnel — auto-created tasks" colspan="2">
                        <field name="mudon_after_sales_task_ids" nolabel="1" readonly="1">
                            <list>
                                <field name="name"/>
                                <field name="kind"/>
                                <field name="state"/>
                                <field name="assigned_user_id" optional="hide"/>
                            </list>
                        </field>
                    </group>
                </group>

                <!-- EOI / Booking (this stage + later) -->
                <group string="EOI / Booking" class="mudon_sec mudon_sec_eoi"
                       invisible="mudon_stage_kind_current not in ('eoi', 'won')">
                    <field name="mudon_fully_paid"
                           string="Fully Paid → advance to WON (SPA Signed)"/>
                </group>

                <!-- Meeting (this stage + later) -->
                <group string="Meeting" class="mudon_sec mudon_sec_meeting"
                       invisible="mudon_stage_kind_current not in ('meeting', 'eoi', 'won')">
                    <field name="mudon_client_type"/>
                    <field name="mudon_property_requirements"/>
                    <field name="mudon_paid_booking"
                           string="Paid Booking → advance to EOI / Booking"/>
                </group>

                <!-- Offer Sent (this stage + later) -->
                <group invisible="mudon_stage_kind_current not in ('offer_sent', 'meeting', 'eoi', 'won')">
                    <group string="Offer Sent" class="mudon_sec mudon_sec_offer_sent">
                        <label for="mudon_offer_counter" string="Offers sent"/>
                        <div class="mudon_offer_row d-flex flex-wrap align-items-center">
                            <field name="mudon_offer_counter" readonly="1" nolabel="1"
                                   class="mudon_offer_count"/>
                            <span class="text-muted ms-2">/ 10</span>
                            <button name="action_mudon_mark_offer_sent" type="object"
                                    string="+ Mark New Offer Sent"
                                    class="btn btn-primary ms-md-3 mt-2 mt-md-0"
                                    confirm="Confirm a new offer was sent to the client. The counter will increment by 1 and a 3-hour follow-up reminder will be queued."/>
                        </div>
                        <field name="mudon_seriousness"/>
                        <field name="mudon_last_offer_date" readonly="1"/>
                        <field name="mudon_visit_confirmed" widget="boolean_toggle"
                               string="Visit Confirmed → advance to Meeting"/>
                    </group>
                    <group string="3rd-offer survey" class="mudon_sec mudon_sec_offer_sent"
                           invisible="not mudon_3rd_offer_survey_sent">
                        <field name="mudon_3rd_offer_survey_sent" invisible="1"/>
                        <field name="mudon_survey_received_offer"
                               invisible="not mudon_3rd_offer_survey_sent"/>
                        <field name="mudon_survey_suitable"
                               invisible="not mudon_3rd_offer_survey_sent"/>
                        <field name="mudon_survey_needs_support"
                               invisible="not mudon_3rd_offer_survey_sent"/>
                        <field name="mudon_survey_property_options"
                               invisible="not mudon_3rd_offer_survey_sent"/>
                        <field name="mudon_survey_status"
                               invisible="not mudon_3rd_offer_survey_sent"/>
                        <field name="mudon_survey_preferred_support"
                               invisible="not mudon_3rd_offer_survey_sent"/>
                    </group>
                </group>

                <!-- Qualified (this stage + later) -->
                <group string="Qualified" class="mudon_sec mudon_sec_qualified"
                       invisible="mudon_stage_kind_current not in ('qualified', 'offer_sent', 'meeting', 'eoi', 'won')">
                    <field name="mudon_tick_offer_sent"
                           string="Offer Sent → advance to Offer Sent"/>
                </group>

                <!-- New Lead intake (always visible; the heading flips per stage) -->
                <separator string="New Lead — capture the enquiry" class="mudon_sec mudon_sec_new_lead"
                           invisible="not mudon_pipeline_kind or mudon_stage_kind_current != 'new_lead'"/>
                <separator string="Lead details" class="mudon_sec mudon_sec_new_lead"
                           invisible="not mudon_pipeline_kind or mudon_stage_kind_current == 'new_lead'"/>
                <group invisible="not mudon_pipeline_kind">
                    <group>
                        <field name="mudon_service_id" options="{'no_create_edit': True}"
                               domain="[('pipeline_kind', 'in', [mudon_pipeline_kind, 'any'])]"
                               required="mudon_pipeline_kind and mudon_stage_kind_current != 'new_lead'"/>
                        <field name="mudon_city_id" options="{'no_create_edit': True}"
                               domain="[('pipeline_kind', 'in', [mudon_pipeline_kind, 'any'])]"
                               required="mudon_pipeline_kind and mudon_stage_kind_current != 'new_lead'"/>
                        <field name="mudon_city_other"
                               invisible="not mudon_show_city_other"/>
                        <field name="mudon_priority"
                               required="mudon_pipeline_kind and mudon_stage_kind_current != 'new_lead'"/>
                        <field name="expected_revenue"
                               options="{'currency_field': 'mudon_budget_currency_id'}"
                               required="mudon_pipeline_kind and mudon_stage_kind_current != 'new_lead'"/>
                        <!--
                            Affordability, asked for in the 2026-08-10 round
                            (field change 13). Sits with the budget because
                            that is the question it qualifies.
                        -->
                        <field name="mudon_full_amount_ready" widget="radio"
                               options="{'horizontal': true}"/>
                        <field name="mudon_ready_amount"
                               options="{'currency_field': 'mudon_budget_currency_id'}"/>
                        <field name="mudon_monthly_amount"
                               options="{'currency_field': 'mudon_budget_currency_id'}"/>
                    </group>
                    <group>
                        <field name="mudon_nationality_id" options="{'no_create': True}"/>
                        <field name="mudon_living_in_id" options="{'no_create': True}"/>
                        <field name="mudon_living_city_id"
                               options="{'no_create_edit': True}"
                               domain="[('country_id', 'in', [mudon_living_in_id, False])]"/>
                        <label for="mudon_in_country" string="In Turkey Now"
                               invisible="mudon_pipeline_kind != 'turkey'"/>
                        <label for="mudon_in_country" string="In UAE Now"
                               invisible="mudon_pipeline_kind != 'uae'"/>
                        <field name="mudon_in_country" nolabel="1"/>
                        <field name="mudon_branch_id" readonly="1"/>
                        <field name="mudon_visit_date"/>
                    </group>
                    <group string="Requirements" colspan="2" class="mudon_sec mudon_sec_new_lead">
                        <field name="mudon_purpose_ids" widget="many2many_tags"
                               options="{'no_create_edit': True, 'color_field': 'color'}"
                               domain="[('pipeline_kind', 'in', [mudon_pipeline_kind, 'any'])]"/>
                        <field name="mudon_property_type_ids" widget="many2many_tags"
                               options="{'no_create_edit': True, 'color_field': 'color'}"/>
                        <field name="mudon_property_area_ids" widget="many2many_tags"
                               options="{'no_create_edit': True, 'color_field': 'color'}"
                               domain="[('pipeline_kind', 'in', [mudon_pipeline_kind, 'any'])]"/>
                        <!-- Qty first, then Beds under it, as the client drew it. -->
                        <field name="mudon_property_qty"/>
                        <field name="mudon_beds_ids" widget="many2many_tags"
                               options="{'no_create_edit': True, 'color_field': 'color'}"/>
                        <field name="mudon_other_specs"/>
                        <field name="mudon_notes"/>
                    </group>
                </group>

                <!-- Move to Lost (escape hatch from any funnel stage) -->
                <group string="Move to Lost" class="mudon_sec mudon_sec_lost"
                       invisible="not mudon_pipeline_kind or mudon_stage_kind_current in ('won', 'lost')">
                    <field name="mudon_lost_reason_id" string="Lost Reason"
                           options="{'no_create_edit': True}"/>
                </group>

                <!-- Dev-only SLA / cron tracking; hidden via group_no_one. -->
                <group string="SLA / Cron Tracking" groups="base.group_no_one"
                       invisible="not mudon_pipeline_kind">
                    <field name="mudon_first_contact_logged"/>
                    <field name="mudon_sla_30min_fired"/>
                    <field name="mudon_sla_1hour_fired"/>
                    <field name="mudon_sla_2hour_fired"/>
                    <field name="mudon_qualified_entry_date"/>
                    <field name="mudon_offer_3hr_reminder_idx"/>
                    <field name="mudon_last_15day_reminder_date"/>
                    <field name="mudon_meeting_reminder_3day_sent"/>
                    <field name="mudon_meeting_reminder_2day_sent"/>
                    <field name="mudon_meeting_reminder_1day_sent"/>
                </group>
            </xpath>


            <!--
                Client feedback: "2 budget boxes (Expected Revenue + Budget),
                why 2?". The custom Budget field carries the per-pipeline
                currency (AED for UAE / USD for Turkey) which Odoo's native
                Expected Revenue can't, so Budget is the money field of
                record. Hide the native Expected Revenue on this form to
                remove the duplicate (field kept in DB — reporting untouched).
            -->
            <!--
                Field change 2: "move Call Status & Source to top right of
                the card". That is the group holding Salesperson, and
                `date_deadline` is the only field unique to it — both
                salesperson groups look otherwise identical, so anchoring on
                user_id would land in the lead-type one half the time.
            -->
            <xpath expr="//field[@name='date_deadline']/ancestor::group[1]"
                   position="inside">
                <field name="mudon_status_id" options="{'no_create_edit': True}"
                       invisible="not mudon_pipeline_kind"/>
                <field name="mudon_source_id" options="{'no_create_edit': True}"
                       invisible="not mudon_pipeline_kind"/>
            </xpath>

            <xpath expr="//label[@for='expected_revenue']" position="attributes">
                <attribute name="invisible">1</attribute>
            </xpath>
            <xpath expr="//field[@name='expected_revenue'][@class='o_input_13ch']" position="attributes">
                <attribute name="invisible">1</attribute>
            </xpath>

            <!--
                Comment 30 — "Remove from inside the card: at Probability
                0.00 %". That whole strip is the stock <h2> banner (native
                revenue + recurring revenue + the AI probability widget).
                Mudon carries its own Budget field and does not use Odoo's
                predictive scoring, so the banner is hidden wholesale for
                Mudon leads. Non-Mudon pipelines keep it untouched.
            -->
            <xpath expr="//div[@id='probability']/.." position="attributes">
                <attribute name="invisible">mudon_pipeline_kind</attribute>
            </xpath>
            <xpath expr="//span[@groups='!crm.group_use_recurring_revenues']" position="attributes">
                <attribute name="invisible">mudon_pipeline_kind</attribute>
            </xpath>

            <!--
                Comment 20 — "Expected Closing? To be removed" (and the
                priority stars sitting beside it, which duplicate Mudon's
                own Urgent/Normal priority).
            -->
            <xpath expr="//label[@for='date_deadline']" position="attributes">
                <attribute name="invisible">mudon_pipeline_kind</attribute>
            </xpath>
            <xpath expr="//field[@name='date_deadline']" position="attributes">
                <attribute name="invisible">mudon_pipeline_kind</attribute>
            </xpath>
            <xpath expr="//field[@name='priority'][@nolabel='1']" position="attributes">
                <attribute name="invisible">mudon_pipeline_kind</attribute>
            </xpath>
            <!--
                The two fields above sit inside a wrapping <div>, and that div
                is a grid cell in its own right. Hiding only the label and the
                fields left an empty cell behind with no label cell to pair
                with, which knocked every following field in this group one
                column out of step — Call Status and Source rendered with
                their labels above and beside their inputs. Hide the cell too.
            -->
            <xpath expr="//field[@name='date_deadline']/parent::div" position="attributes">
                <attribute name="invisible">mudon_pipeline_kind</attribute>
            </xpath>

            <!--
                Comment 21 — "What is Tags?". Odoo's generic CRM tags are
                unused here: Mudon classifies with Service / City / Purpose /
                Property Type masters instead, so the field is just noise.
            -->
            <xpath expr="//field[@name='tag_ids']" position="attributes">
                <attribute name="invisible">mudon_pipeline_kind</attribute>
            </xpath>

            <!--
                HEADER — enterprise stage controls. The stock CRM "Won" and
                "Lost" header buttons bypass Mudon's stage gates (Won) or
                ARCHIVE the lead (Lost → active=False, so it vanishes from
                the pipeline). Hide them for Mudon leads and provide our own
                Advance / Mark Lost / Restore that route through the gated
                stage flow (quick-fill wizard when a gate field is missing).
            -->
            <xpath expr="//header/button[@data-hotkey='w']" position="attributes">
                <attribute name="invisible">mudon_pipeline_kind or won_status == 'won' or type == 'lead' or not active</attribute>
            </xpath>
            <xpath expr="//header/button[@data-hotkey='l']" position="attributes">
                <attribute name="invisible">mudon_pipeline_kind or won_status != 'pending' or not active</attribute>
            </xpath>
            <!--
                HEADER BUTTON ORDER + COLOURS — client comment 24 supplied a
                mock-up: "← Back to Pipeline | Delete | Mark Lost | Advance",
                with Advance as the green primary action on the right. XML
                order is render order, so the buttons are declared in exactly
                the sequence he drew.
            -->
            <xpath expr="//header/field[@name='stage_id']" position="before">
                <!--
                    Zero-height marker. It renders only on a Mudon lead, which
                    lets the stylesheet colour this form's stage bar to match
                    the kanban stage colours (comment 10) via :has(), without
                    touching any other CRM pipeline in the database.
                -->
                <div class="mudon_form_marker d-none"
                     invisible="not mudon_pipeline_kind"/>
                <button name="action_mudon_back_to_pipeline" type="object"
                        string="← Back to Pipeline" class="btn-link mudon_btn_back"
                        invisible="not mudon_pipeline_kind"/>
                <button name="action_mudon_delete_lead" type="object"
                        string="Delete" class="btn-outline-danger mudon_btn_delete"
                        confirm="Delete this lead permanently? This action cannot be undone."
                        invisible="not mudon_pipeline_kind"
                        groups="mudon_crm.group_mudon_super_admin"/>
                <button name="action_mudon_mark_lost" type="object"
                        string="Mark Lost" class="btn-outline-secondary mudon_btn_lost"
                        invisible="not mudon_pipeline_kind or mudon_stage_kind_current in ('won', 'lost')"/>
                <button name="action_mudon_kanban_advance" type="object"
                        string="Advance" class="btn-success mudon_btn_advance"
                        data-hotkey="n"
                        invisible="not mudon_pipeline_kind or not mudon_is_funnel_stage or mudon_stage_kind_current in ('won', 'lost')"/>
                <button name="action_mudon_reopen" type="object"
                        string="Restore" class="btn-success mudon_btn_advance"
                        invisible="mudon_stage_kind_current != 'lost'"/>
            </xpath>

            <!-- WON / LOST ribbons (Mudon uses its own stages, not won_status). -->
            <xpath expr="//div[@name='button_box']" position="after">
                <widget name="web_ribbon" title="WON" bg_color="text-bg-success"
                        invisible="mudon_stage_kind_current != 'won'"/>
                <widget name="web_ribbon" title="LOST" bg_color="text-bg-danger"
                        invisible="mudon_stage_kind_current != 'lost'"/>
            </xpath>

            <!-- Stat buttons: drill into the funnel tasks this deal spawned. -->
            <xpath expr="//div[@name='button_box']" position="inside">
                <button type="object" name="action_mudon_open_admin_tasks"
                        class="oe_stat_button" icon="fa-tasks"
                        invisible="mudon_admin_task_count == 0">
                    <field name="mudon_admin_task_count" widget="statinfo" string="Admin Tasks"/>
                </button>
                <button type="object" name="action_mudon_open_after_sales_tasks"
                        class="oe_stat_button" icon="fa-handshake-o"
                        invisible="mudon_after_sales_task_count == 0">
                    <field name="mudon_after_sales_task_count" widget="statinfo" string="After-Sales"/>
                </button>
            </xpath>
        </field>
    </record>

    <!--
        Color-coded kanban. The Mudon priority rank goes into kanban_state
        / record_top via the computed mudon_card_color_hint badge so the
        4-tier sort (P1 → P4) is visible without touching the upstream
        kanban template structure.
    -->
    <!--
        Client: "Turkey card is showing AED while must show USD".
        The stock list renders the budget with `company_currency`, and the
        company currency is AED, so every list - including the dashboard
        drill-throughs - showed Turkey amounts in dirhams. The kanban and
        the form were corrected long ago; the list was missed.
    -->
    <record id="mudon_crm_lead_view_list" model="ir.ui.view">
        <field name="name">crm.lead.list.mudon</field>
        <field name="model">crm.lead</field>
        <field name="inherit_id" ref="crm.crm_case_tree_view_oppor"/>
        <field name="arch" type="xml">
            <field name="expected_revenue" position="attributes">
                <attribute name="options">{'currency_field': 'mudon_budget_currency_id'}</attribute>
            </field>
            <field name="expected_revenue" position="before">
                <field name="mudon_budget_currency_id" column_invisible="1"/>
            </field>
        </field>
    </record>

    <record id="mudon_crm_lead_view_kanban" model="ir.ui.view">
        <field name="name">crm.lead.kanban.mudon</field>
        <field name="model">crm.lead</field>
        <field name="inherit_id" ref="crm.crm_case_kanban_view_leads"/>
        <field name="arch" type="xml">
            <!--
                Comment 8 — the quick-create popup must ask for Name, Phone,
                Email and Amount only (no Company / "Opportunity's Name").
            -->
            <xpath expr="//kanban" position="attributes">
                <attribute name="quick_create_view">mudon_crm.mudon_crm_lead_view_quick_create</attribute>
            </xpath>

            <!--
                Comment 23 - the column header totalled `expected_revenue`,
                whose currency_field is the COMPANY currency, so the Dubai
                board added up AED amounts and printed them with a $. Sum the
                pipeline-currency mirror instead so each board totals in its
                own currency. The record count next to the column title comes
                from the KanbanHeader template inherit.
            -->
            <xpath expr="//progressbar" position="attributes">
                <attribute name="sum_field">mudon_pipeline_revenue</attribute>
            </xpath>

            <!--
                ══ Comment 28 — card clean-up ══════════════════════════════
                The client marked four things on the card as duplicates to
                REMOVE. Every one of them is a stock CRM element that the
                Mudon block below already renders in its own styling:

                  * "$ 200,000.00" strip  -> shown in company currency (USD),
                    which is what made the Dubai cards read "$" in comment 4;
                    the Mudon block shows it in the pipeline currency (AED).
                  * contact chip "T TEst117 TR7" -> repeats the card title.
                  * the "M" agent avatar in the footer -> the Mudon footer
                    already carries the agent avatar.
                  * the priority stars -> duplicate Mudon's Urgent / Normal.

                All are hidden for Mudon leads only; other pipelines keep the
                stock card exactly as it is.
            -->
            <xpath expr="//div[@class='o_kanban_card_crm_lead_revenue']" position="attributes">
                <attribute name="invisible">mudon_pipeline_kind</attribute>
            </xpath>
            <xpath expr="//field[@name='partner_id'][@widget='many2one_avatar']/.." position="attributes">
                <attribute name="invisible">mudon_pipeline_kind or not partner_id</attribute>
            </xpath>
            <!--
                Client question: "What is this contact field? Is this the
                sales agent or the customer name?" It was `partner_id`, the
                link to an address-book record, which the Mudon team never
                creates, so it always looked empty. Show the customer's own
                name instead, and keep the address-book link out of the way.
            -->
            <xpath expr="//field[@name='contact_name']" position="attributes">
                <attribute name="invisible">not mudon_pipeline_kind and partner_id</attribute>
                <attribute name="string">Customer Name</attribute>
            </xpath>
            <xpath expr="//field[@name='partner_name']" position="attributes">
                <attribute name="invisible">mudon_pipeline_kind or partner_id or contact_name</attribute>
            </xpath>
            <xpath expr="//field[@name='tag_ids']" position="attributes">
                <attribute name="invisible">mudon_pipeline_kind</attribute>
            </xpath>
            <xpath expr="//field[@name='priority'][@widget='priority']" position="attributes">
                <attribute name="invisible">mudon_pipeline_kind</attribute>
            </xpath>
            <xpath expr="//field[@name='user_id'][@widget='many2one_avatar_user']" position="attributes">
                <attribute name="invisible">mudon_pipeline_kind</attribute>
            </xpath>

            <xpath expr="//templates" position="before">
                <field name="mudon_pipeline_kind"/>
                <field name="mudon_service_id"/>
                <field name="mudon_service_code"/>
                <field name="mudon_priority"/>
                <field name="mudon_status_id"/>
                <field name="mudon_card_color_hint"/>
                <field name="mudon_kanban_priority_rank"/>
                <field name="mudon_stage_kind_current"/>
                <field name="mudon_is_funnel_stage"/>
                <field name="mudon_offer_counter"/>
                <field name="expected_revenue"/>
                <field name="mudon_budget_currency_id"/>
                <field name="mudon_visit_date"/>
                <field name="stage_id"/>
                <field name="user_id"/>
                <field name="phone"/>
                <field name="mudon_wa_phone"/>
                <field name="mudon_first_contact_logged"/>
                <field name="mudon_status_id"/>
                <field name="mudon_source_id"/>
                <field name="mudon_pipeline_revenue"/>
                <field name="partner_id"/>
                <field name="contact_name"/>
            </xpath>
            <!--
                The card printed the person's name twice. Odoo shows
                `contact_name` under the title whenever there is no linked
                contact, and for a Mudon lead the two are the same value:
                create() copies the title into the contact field so the
                Customer Name is never blank, which is what the client
                asked for. Keep that, and just stop the card repeating it.
            -->
            <xpath expr="//field[@name='contact_name']" position="attributes">
                <attribute name="invisible">partner_id or contact_name == name</attribute>
            </xpath>
            <!--
                v19.0.1.2.0 — styled Mudon card block: a stage-coloured
                band header + a priority-coloured left accent + native
                coloured tags for City / Property Type. The block carries
                its own colour coding so it reads at a glance regardless of
                the upstream card chrome. Injected just before <footer> (a
                stable anchor in the Odoo 18+ kanban card structure).
            -->
            <xpath expr="//footer" position="before">
                <div t-if="record.mudon_pipeline_kind.raw_value"
                     t-attf-class="mudon_card_block mudon_prio_#{record.mudon_priority.raw_value or 'none'}">
                    <!-- Stage band header (colour by stage kind) -->
                    <div t-attf-class="mudon_band mudon_band_#{record.mudon_stage_kind_current.raw_value or 'new_lead'}">
                        <t t-out="record.stage_id.value"/>
                    </div>
                    <!-- Priority + service badges -->
                    <div class="mudon_badges mt-1">
                        <span t-if="record.mudon_priority.raw_value == 'urgent'"
                              class="badge text-bg-danger me-1">🔴 Urgent</span>
                        <span t-if="record.mudon_priority.raw_value == 'normal'"
                              class="badge text-bg-warning me-1">🟡 Normal</span>
                        <span t-if="record.mudon_service_code.raw_value == 'investment'"
                              class="badge text-bg-primary me-1">🟦 Investment</span>
                        <span t-if="record.mudon_service_code.raw_value == 'citizenship'"
                              class="badge text-bg-success me-1">🟩 Citizenship</span>
                        <span t-if="record.mudon_service_code.raw_value == 'goldenvisa'"
                              class="badge text-bg-success me-1">🟩 Golden Visa</span>
                    </div>
                    <!-- Coloured tags: city + property type -->
                    <div class="mudon_tags mt-1">
                        <field name="mudon_city_id"/>
                        <field name="mudon_property_type_ids" widget="many2many_tags"
                               options="{'color_field': 'color'}"/>
                    </div>
                    <!-- Expected revenue + offers -->
                    <div class="mudon_meta mt-1 small text-muted">
                        <span t-if="record.expected_revenue.raw_value" class="me-2">
                            💰 <field name="expected_revenue" widget="monetary"
                                      options="{'currency_field': 'mudon_budget_currency_id'}"/>
                        </span>
                        <span t-if="record.mudon_offer_counter.raw_value"
                              class="badge text-bg-secondary">
                            Offers: <t t-out="record.mudon_offer_counter.value"/>
                        </span>
                    </div>
                    <!--
                        Comment 28 "Add" column: Status, Source and Expected
                        Visit Date on the card face. Comment 19 asks for the
                        visit date specifically once a card reaches Meeting,
                        so it is highlighted from that stage onward.
                    -->
                    <div class="mudon_cardinfo mt-1 small">
                        <div t-if="record.mudon_status_id.raw_value" class="mudon_ci">
                            <span class="mudon_ci_lbl">Status</span>
                            <span class="mudon_ci_val"><t t-out="record.mudon_status_id.value"/></span>
                        </div>
                        <div t-if="record.mudon_source_id.raw_value" class="mudon_ci">
                            <span class="mudon_ci_lbl">Source</span>
                            <span class="mudon_ci_val"><t t-out="record.mudon_source_id.value"/></span>
                        </div>
                        <div t-if="record.mudon_visit_date.raw_value"
                             t-attf-class="mudon_ci #{record.mudon_stage_kind_current.raw_value == 'meeting' ? 'mudon_ci_hi' : ''}">
                            <span class="mudon_ci_lbl">Expected Visit</span>
                            <span class="mudon_ci_val"><t t-out="record.mudon_visit_date.value"/></span>
                        </div>
                    </div>
                    <!-- Card footer: agent avatar + SLA chip + one-tap WhatsApp
                         (the Gulf real-estate team lives in WhatsApp). -->
                    <div class="mudon_cardfoot d-flex align-items-center justify-content-between mt-1 px-2 pb-1">
                        <div class="d-flex align-items-center gap-1">
                            <field name="user_id" widget="many2one_avatar_user"/>
                            <span t-if="!record.mudon_first_contact_logged.raw_value and ['new_lead','qualified'].includes(record.mudon_stage_kind_current.raw_value)"
                                  class="badge text-bg-danger">Not contacted</span>
                        </div>
                        <a t-if="record.mudon_wa_phone.raw_value"
                           t-attf-href="https://wa.me/#{record.mudon_wa_phone.raw_value}"
                           target="_blank" class="btn btn-sm btn-success mudon_wa_btn"
                           title="Chat on WhatsApp">
                            <i class="fa fa-whatsapp"/>
                        </a>
                    </div>
                    <!-- Touch stage-move (mobile/tablet can't drag) — same
                         gate + quick-fill wizard as a drag. Only rendered on
                         real funnel stages (+ Lost, for reopen). -->
                    <div class="mudon_stage_move"
                         t-if="record.mudon_is_funnel_stage.raw_value or record.mudon_stage_kind_current.raw_value == 'lost'">
                        <button type="object" name="action_mudon_kanban_back"
                                class="btn btn-sm btn-light mudon_move_btn"
                                t-if="(record.mudon_is_funnel_stage.raw_value and record.mudon_stage_kind_current.raw_value != 'new_lead') or record.mudon_stage_kind_current.raw_value == 'lost'"
                                aria-label="Move back a stage" title="Move back a stage">
                            <i class="fa fa-chevron-left me-1"/><t t-out="record.mudon_stage_kind_current.raw_value == 'lost' ? 'Reopen' : 'Back'"/>
                        </button>
                        <button type="object" name="action_mudon_kanban_advance"
                                class="btn btn-sm btn-primary mudon_move_btn"
                                t-if="record.mudon_is_funnel_stage.raw_value and record.mudon_stage_kind_current.raw_value != 'won'"
                                aria-label="Advance to next stage" title="Advance to next stage">
                            Advance<i class="fa fa-chevron-right ms-1"/>
                        </button>
                    </div>
                </div>
            </xpath>
        </field>
    </record>

    <record id="mudon_crm_lead_view_search" model="ir.ui.view">
        <field name="name">crm.lead.search.mudon</field>
        <field name="model">crm.lead</field>
        <!--
            Inherit the OPPORTUNITY filter, not the lead one. Odoo picks the
            primary search view with the lowest priority, and
            crm.lead.search.opportunity is priority 15 against the lead
            filter's 16, so everything added below hung off a view the Mudon
            menus never load. The filters were all present in the file and
            none of them had ever appeared on screen.
        -->
        <field name="inherit_id" ref="crm.view_crm_case_opportunities_filter"/>
        <field name="arch" type="xml">
            <!--
                ══ Comment 7 — stage filter leaked across pipelines ════════
                "when filtering stage while in Dubai pipeline, it shows all
                the item of both pipelines" — the Search:Stage dialog listed
                all 14 stages because the stage field carried no domain. Each
                pipeline action puts its team in `default_team_id`, so scope
                the lookup to that team when one is present, and fall back to
                the unfiltered list on the All Mudon Leads view.

                The stock search view has no Stage field at all, which is why
                the client reached Stage through Add Custom Filter and got the
                unscoped picker. Adding it here gives a scoped Stage straight
                from the search bar; crm_stage.py scopes the picker itself so
                the custom-filter route is covered too.
            -->
            <!--
                ══ Comment 21 — "Can I archive?" ═══════════════════════════
                Archiving a lost card works, but Odoo hides archived records
                and the client had no way to see them again without knowing
                to build a custom filter.
            -->
            <xpath expr="//search" position="inside">
                <separator/>
                <filter name="mudon_archived" string="Archived"
                        domain="[('active', '=', False)]"/>
            </xpath>

            <xpath expr="//search" position="inside">
                <field name="stage_id" string="Stage"
                       domain="context.get('default_team_id') and [('team_ids', '=', context.get('default_team_id'))] or []"/>
            </xpath>

            <!--
                ══ Comment 22 — "Make search bar possible by default to all
                criteria of the fields" ═══════════════════════════════════
                Every field the client listed is registered here, so the
                search bar offers it as you type and Add Custom Filter can
                reach it. The Yes/No filters cover the tick-box fields he
                wanted searchable: a Boolean is already searchable in Odoo,
                so these give the Yes/No choice he asked for without a risky
                change to the stored field type.
            -->
            <xpath expr="//search" position="inside">
                <field name="mudon_service_id" string="Service"/>
                <field name="mudon_city_id" string="City"/>
                <field name="mudon_priority" string="Priority"/>
                <field name="mudon_status_id" string="Call Status"/>
                <field name="mudon_source_id" string="Source"/>
                <field name="mudon_nationality_id" string="Nationality"/>
                <field name="mudon_living_in_id" string="Living Country"/>
                <field name="mudon_living_city_id" string="Living City"/>
                <field name="mudon_branch_id" string="Branch"/>
                <field name="mudon_beds_ids" string="No. of Beds"/>
                <field name="mudon_property_qty" string="Property Qty"/>
                <field name="mudon_property_area_ids" string="Property Area"/>
                <field name="mudon_full_amount_ready" string="Full Amount Ready"/>
                <field name="mudon_ready_amount" string="Ready Amount"/>
                <field name="mudon_monthly_amount" string="Available Monthly Amount"/>
                <field name="mudon_visit_date" string="Expected Visit Date"/>
                <field name="mudon_purpose_ids" string="Purpose of the Property"/>
                <field name="mudon_property_type_ids" string="Property Type"/>
                <field name="mudon_other_specs" string="Other Specifications"/>
                <field name="mudon_notes" string="Notes"/>
                <field name="mudon_offer_counter" string="Offers Sent"/>
                <field name="mudon_seriousness" string="Seriousness"/>
                <field name="mudon_last_offer_date" string="Last Offer Sent At"/>
                <field name="mudon_client_type" string="Client Type"/>
                <field name="mudon_property_requirements" string="Property Requirements"/>
                <field name="mudon_closing_amount" string="Closing Amount" groups="mudon_crm.group_mudon_financial_data"/>
                <field name="mudon_developer_id" string="Developer"/>
                <field name="mudon_project_id" string="Project"/>
                <field name="mudon_commission" string="Commission" groups="mudon_crm.group_mudon_financial_data"/>
                <field name="mudon_commission_pct" string="Commission %" groups="mudon_crm.group_mudon_financial_data"/>
                <field name="mudon_commission_type_id" string="Commission Type"/>
                <field name="mudon_handover_type" string="Handover Type"/>
                <field name="mudon_reason_to_win" string="Reason to Win"/>
                <field name="mudon_invoiced_amount" string="Invoiced / Commission Billed" groups="mudon_crm.group_mudon_financial_data"/>
                <field name="mudon_invoiced_date" string="Invoice Date"/>
                <field name="mudon_collected_amount" string="Collected / Cash In" groups="mudon_crm.group_mudon_financial_data"/>
                <field name="mudon_collected_date" string="Payment Received Date"/>
                <field name="mudon_lost_reason_id" string="Lost Reason"/>

                <!--
                    Comment 22 - Odoo's search bar cannot offer a Boolean for
                    free-text search, which is why the tick-box fields never
                    appeared in the type-ahead list. These stored Yes/No
                    mirrors are searchable and groupable; the underlying
                    Booleans still drive every stage gate.
                -->
                <field name="mudon_in_country_yn" string="In Turkey / UAE Now"/>
                <field name="mudon_tick_offer_sent_yn" string="Offer Sent"/>
                <field name="mudon_visit_confirmed_yn" string="Visit Confirmed"/>
                <field name="mudon_paid_booking_yn" string="Paid Booking"/>
                <field name="mudon_fully_paid_yn" string="Fully Paid"/>
                <field name="mudon_need_invoice_yn" string="Need Invoice"/>
                <field name="mudon_title_deed_required_yn" string="Title Deed Required"/>
                <field name="mudon_citizenship_required_yn" string="Citizenship Required"/>
                <field name="mudon_residence_required_yn" string="Residence Required"/>
                <field name="mudon_furniture_required_yn" string="Furniture / Other Required"/>

                <!-- Yes / No searching for every tick-box field. -->
                <separator/>
                <filter name="mudon_in_country_yes" string="In Turkey / UAE Now: Yes"
                        domain="[('mudon_in_country', '=', True)]"/>
                <filter name="mudon_in_country_no" string="In Turkey / UAE Now: No"
                        domain="[('mudon_in_country', '=', False)]"/>
                <separator/>
                <filter name="mudon_offer_yes" string="Offer Sent: Yes"
                        domain="[('mudon_tick_offer_sent', '=', True)]"/>
                <filter name="mudon_offer_no" string="Offer Sent: No"
                        domain="[('mudon_tick_offer_sent', '=', False)]"/>
                <filter name="mudon_visit_yes" string="Visit Confirmed: Yes"
                        domain="[('mudon_visit_confirmed', '=', True)]"/>
                <filter name="mudon_visit_no" string="Visit Confirmed: No"
                        domain="[('mudon_visit_confirmed', '=', False)]"/>
                <filter name="mudon_booking_yes" string="Paid Booking: Yes"
                        domain="[('mudon_paid_booking', '=', True)]"/>
                <filter name="mudon_booking_no" string="Paid Booking: No"
                        domain="[('mudon_paid_booking', '=', False)]"/>
                <filter name="mudon_paid_yes" string="Fully Paid: Yes"
                        domain="[('mudon_fully_paid', '=', True)]"/>
                <filter name="mudon_paid_no" string="Fully Paid: No"
                        domain="[('mudon_fully_paid', '=', False)]"/>
                <separator/>
                <filter name="mudon_invoice_yes" string="Need Invoice: Yes"
                        domain="[('mudon_need_invoice', '=', True)]"/>
                <filter name="mudon_deed_yes" string="Title Deed Required: Yes"
                        domain="[('mudon_title_deed_required', '=', True)]"/>
                <filter name="mudon_citizenship_yes" string="Citizenship Required: Yes"
                        domain="[('mudon_citizenship_required', '=', True)]"/>
                <filter name="mudon_residence_yes" string="Residence Required: Yes"
                        domain="[('mudon_residence_required', '=', True)]"/>
                <filter name="mudon_furniture_yes" string="Furniture / Other Required: Yes"
                        domain="[('mudon_furniture_required', '=', True)]"/>

                <separator/>
                <filter name="mudon_turkey" string="Mudon Turkey"
                        domain="[('mudon_pipeline_kind', '=', 'turkey')]"/>
                <filter name="mudon_uae" string="Mudon UAE Dubai"
                        domain="[('mudon_pipeline_kind', '=', 'uae')]"/>
                <separator/>
                <filter name="mudon_my" string="My Pipeline"
                        domain="[('user_id', '=', uid)]"/>
                <filter name="mudon_overdue_sla" string="Overdue SLA"
                        domain="[('mudon_pipeline_kind', 'in', ('turkey', 'uae')),
                                 ('mudon_first_contact_logged', '=', False),
                                 '|', ('mudon_sla_1hour_fired', '=', True),
                                      ('mudon_sla_2hour_fired', '=', True)]"/>
                <filter name="mudon_won_month" string="Won this month"
                        domain="[('mudon_stage_kind_current', '=', 'won'),
                                 ('date_closed', '>=', context_today().replace(day=1).strftime('%Y-%m-%d 00:00:00'))]"/>
                <separator/>
                <filter name="mudon_urgent" string="Urgent"
                        domain="[('mudon_priority', '=', 'urgent')]"/>
                <filter name="mudon_p1" string="P1 — Urgent + Citizenship/GV"
                        domain="[('mudon_card_color_hint', '=', 'urgent_special')]"/>
                <filter name="mudon_p2" string="P2 — Urgent + Investment"
                        domain="[('mudon_card_color_hint', '=', 'urgent_invest')]"/>
                <filter name="mudon_p3" string="P3 — Normal + Citizenship/GV"
                        domain="[('mudon_card_color_hint', '=', 'normal_special')]"/>
                <filter name="mudon_p4" string="P4 — Normal + Investment"
                        domain="[('mudon_card_color_hint', '=', 'normal_invest')]"/>
                <separator/>
                <filter name="mudon_not_contacted"
                        string="Not contacted yet"
                        domain="[
                            ('mudon_first_contact_logged', '=', False),
                            ('mudon_pipeline_kind', 'in', ('turkey', 'uae'))]"/>
                <filter name="mudon_high_offers" string="Offers ≥ 3"
                        domain="[('mudon_offer_counter', '>=', 3)]"/>
                <separator/>
                <filter name="mudon_group_stage" string="Stage"
                        context="{'group_by': 'stage_id'}"/>
                <filter name="mudon_group_service" string="Service"
                        context="{'group_by': 'mudon_service_id'}"/>
                <filter name="mudon_group_status" string="Status"
                        context="{'group_by': 'mudon_status_id'}"/>
                <filter name="mudon_group_branch" string="Branch"
                        context="{'group_by': 'mudon_branch_id'}"/>
                <filter name="mudon_group_source" string="Source"
                        context="{'group_by': 'mudon_source_id'}"/>
                <filter name="mudon_group_agent" string="Agent"
                        context="{'group_by': 'user_id'}"/>
                <filter name="mudon_group_nationality" string="Nationality"
                        context="{'group_by': 'mudon_nationality_id'}"/>
            </xpath>
        </field>
    </record>
</odoo>
